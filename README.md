# EvilBit-Labs Tap

## How do I install these formulae?

`brew install evilbit-labs/tap/<formula>`

Or `brew tap evilbit-labs/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "evilbit-labs/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
